// Cinemas themselves are now real, backend-owned Firestore data (see
// src/api/cinemasApi.js and server/src/scripts/seed.js). This file only
// keeps the display city label used in headers before a real location is
// resolved.

export const mockCity = 'Bengaluru'
